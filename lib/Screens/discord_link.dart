import 'package:flutter/material.dart';

class DiscordLink extends StatefulWidget {
  const DiscordLink({Key? key}) : super(key: key);

  @override
  State<DiscordLink> createState() => _DiscordLinkState();
}

class _DiscordLinkState extends State<DiscordLink> {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Discord Link'));
  }
}
